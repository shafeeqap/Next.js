/* Group route (You can access directly profile settings page like this 'http://localhost:3000/settings')
   A Group Route is a way to organize your routes without affecting the URL path.
*/

export default function UserSettings() {
  return <h1>User Profile Settings Page</h1>;
}
