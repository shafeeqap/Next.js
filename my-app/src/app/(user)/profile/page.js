/* Group route (You can access directly profile page like this 'http://localhost:3000/profile')
   A Group Route is a way to organize your routes without affecting the URL path.
*/

export default function UserProfile() {
  return <h1>User Profile Page</h1>;
}
